# 🐍 Funções Nativas do Python

Abaixo está uma lista completa das funções nativas (built-in functions) do Python, com breves explicações.

| Função | Explicação curta |
|--------|------------------|
| `abs()` | Retorna o valor absoluto de um número. |
| `all()` | Retorna `True` se todos os elementos forem verdadeiros. |
| `any()` | Retorna `True` se algum elemento for verdadeiro. |
| `ascii()` | Retorna a representação ASCII de um objeto. |
| `bin()` | Converte um número inteiro em binário (`'0b...'`). |
| `bool()` | Converte para tipo booleano (`True`/`False`). |
| `breakpoint()` | Inicia o depurador (`pdb`) no ponto de parada. |
| `bytearray()` | Cria um array mutável de bytes. |
| `bytes()` | Cria um objeto imutável de bytes. |
| `callable()` | Verifica se um objeto pode ser chamado (ex.: função). |
| `chr()` | Converte um número inteiro em caractere Unicode. |
| `classmethod()` | Cria um método de classe. |
| `compile()` | Compila código-fonte em objeto executável. |
| `complex()` | Cria um número complexo. |
| `delattr()` | Remove um atributo de um objeto. |
| `dict()` | Cria um dicionário. |
| `dir()` | Lista os atributos e métodos de um objeto. |
| `divmod()` | Retorna quociente e resto de uma divisão. |
| `enumerate()` | Retorna índices e valores de um iterável. |
| `eval()` | Executa uma expressão Python em string. |
| `exec()` | Executa código Python em string. |
| `filter()` | Filtra elementos com base em uma função. |
| `float()` | Converte para número de ponto flutuante. |
| `format()` | Formata valores em strings. |
| `frozenset()` | Cria um conjunto imutável. |
| `getattr()` | Retorna o valor de um atributo de um objeto. |
| `globals()` | Retorna o dicionário global atual. |
| `hasattr()` | Verifica se o objeto tem um atributo. |
| `hash()` | Retorna o valor de hash de um objeto. |
| `help()` | Exibe ajuda interativa de objetos e funções. |
| `hex()` | Converte número em hexadecimal. |
| `id()` | Retorna o identificador único do objeto. |
| `input()` | Lê entrada do usuário. |
| `int()` | Converte para número inteiro. |
| `isinstance()` | Verifica se o objeto é de um tipo específico. |
| `issubclass()` | Verifica se uma classe herda de outra. |
| `iter()` | Cria um iterador. |
| `len()` | Retorna o tamanho de um objeto. |
| `list()` | Cria uma lista. |
| `locals()` | Retorna variáveis locais do escopo atual. |
| `map()` | Aplica uma função a todos os itens de um iterável. |
| `max()` | Retorna o maior valor. |
| `memoryview()` | Cria uma visão de memória sobre bytes. |
| `min()` | Retorna o menor valor. |
| `next()` | Retorna o próximo item de um iterador. |
| `object()` | Cria um objeto base vazio. |
| `oct()` | Converte número em octal. |
| `open()` | Abre um arquivo. |
| `ord()` | Retorna o código Unicode de um caractere. |
| `pow()` | Calcula potência (xⁿ). |
| `print()` | Exibe saída na tela. |
| `property()` | Cria propriedades em classes. |
| `range()` | Gera uma sequência de números. |
| `repr()` | Retorna representação oficial de um objeto. |
| `reversed()` | Inverte uma sequência. |
| `round()` | Arredonda número para n casas decimais. |
| `set()` | Cria um conjunto. |
| `setattr()` | Define um atributo em um objeto. |
| `slice()` | Cria um objeto de fatiamento. |
| `sorted()` | Retorna uma lista ordenada. |
| `staticmethod()` | Cria um método estático em uma classe. |
| `str()` | Converte para string. |
| `sum()` | Soma elementos numéricos. |
| `super()` | Chama métodos da superclasse. |
| `tuple()` | Cria uma tupla. |
| `type()` | Retorna o tipo de um objeto. |
| `vars()` | Retorna o dicionário `__dict__` de um objeto. |
| `zip()` | Combina iteráveis em pares. |
| `__import__()` | Importa um módulo dinamicamente. |
